export const enum EStepCost {
  Rock = 1,
  Paper = 2,
  Scissors = 3,
}

export const enum EResultCost {
  Lose = 0,
  Draw = 3,
  Win = 6,
}

export type MyCommands = 'X' | 'Y' | 'Z';
export type RivalCommands = 'A' | 'B' | 'C';
